services:
  dataflow:
    build: .
    ports:
      - "3460:3460"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    stop_grace_period: 30s
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '1.0'
